#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);cin.tie(NULL);
    int n;cin>>n;
    cout<<int(sqrt(1+8*n)+(int(sqrt(1+8*n))!=sqrt(1+8*n)))/2;
    return 0;
}
